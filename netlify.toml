[build]
  base = "frontend"
  command = "pnpm build"
  publish = "dist"

# Las reglas de redirección (proxy a Fly + fallback SPA) viven en
# frontend/public/_redirects para garantizar el orden correcto.
